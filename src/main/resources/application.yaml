server:
  port: 8090

spring:
  application:
    name: ms-notificacion

  datasource:
    url: jdbc:postgresql://ep-odd-poetry-acs57uy9-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_8SmyY0IzEFOl
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  threads:
    virtual:
      enabled: true

  mvc:
    problemdetails:
      enabled: false

eureka:
 client:
  register-with-eureka: true
  fetch-registry: true
  service-url:
   defaultZone: https://eureka-server-wp64.onrender.com/eureka/

 instance:
  prefer-ip-address: false
  hostname: ${RENDER_EXTERNAL_HOSTNAME:localhost}
  secure-port-enabled: true
  secure-port: 443
  non-secure-port-enabled: false
  home-page-url: https://${RENDER_EXTERNAL_HOSTNAME}/
  status-page-url: https://${RENDER_EXTERNAL_HOSTNAME}/actuator/info
  health-check-url: https://${RENDER_EXTERNAL_HOSTNAME}/actuator/health

springdoc:
 api-docs:
  enabled: true
 swagger-ui:
  enabled: true
  path: /doc/swagger-ui.html
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000